# Add SDIOS-library to an existing application
In this directory, we will show how simple it is to integrate with our SDIOS-Library.

## viewSensorsOutput
This is a toy project to show sensor output.

Here, we demonstrated how to use the library on SDIOS to interact with the trust value.

Applications can get the service SensorEvent trust when using `registerListenerSdios`.
